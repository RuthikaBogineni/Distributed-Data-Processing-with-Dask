# Distributed-Data-Processing-with-Dask
Automatically generates a large CSV dataset if one is not present.  Implements an ETL pipeline with filtering, groupby, and aggregation  Benchmarks execution time and memory usage for Pandas vs Dask  Uses a single executable Python script with logging and CLI configuration  Designed to be reproducible and production-style
